The following text is a directory structure with code files.

The structure of the text is divided into sections that begin with "----", followed by a single line containing the file path and file name, and then the file contents.

The text representing the directory ends when the symbols "--END--" are encountered.

Any further text beyond "--END--" is meant to be interpreted as instructions using the aforementioned directory structure as context.
